name: Validar site

on:
  pull_request:
  push:
    branches:
      - main
      - site-premium-v2
      - 'fix/**'
      - 'feat/**'

permissions:
  contents: read

jobs:
  site-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validar referências de mídia
        run: node tools/check-media-paths.mjs

      - name: Validar links e interações
        run: node tools/check-interactions.mjs
